//run the harvesters
/*
if(creep.memory.role == 'harvester'){
  harvester.run(creep);
}
if(creep.memory.role == 'miner'){
  miner.run(creep);
}
if(creep.memory.role == 'mover'){
  mover.run(creep);
}
//run the upgraders
else if(creep.memory.role == 'upgrader'){
  upgrader.run(creep);
}
else if(creep.memory.role == 'upgradercon'){
  upgradercon.run(creep);
}
//run the builders
else if(creep.memory.role == 'builder'  || creep.memory.role == 'Builder'){
  builder.run(creep);
}
//run the repairers
else if (creep.memory.role == 'repairer'){
  repairer.run(creep);
}
else if (creep.memory.role == 'wallrepairer'){
  wallrepairer.run(creep);
}
else if (creep.memory.role == 'longDistanceHarvester'){
  longDistanceHarvester.run(creep);
}
*/

//  for(x in runOrder)
//{
  //if(creep.memory.role == runOrder[x]){
  //  var command = runOrder[x] + '.run';
  //  }
//}
//else if(numberOfBuilders < maximumNumberofDefault){
//    name = spawn.createCustomCreep(energy, 'builder');
//  }

/*
else if(numberOfLongDistanceHarvesters < maximumNumberofDefault)
{
  name = Game.spawns.Spawn1.createCustomCreeps(4,6,8, 'longDistanceHarvester',home, '70100773019e434');
}
*/
